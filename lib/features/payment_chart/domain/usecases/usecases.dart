 
    import '../repositories/repositories.dart';

    class GetPayment_chartUseCase {
        final Payment_chartRepository repository;
      
        GetPayment_chartUseCase({required this.repository});
      
        // Future<User> execute(String userId) async {
        //   return userRepository.getUser(userId);
        // }
      }
      